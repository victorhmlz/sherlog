import RoutePlaceholder from "@/components/ui/RoutePlaceholder";

export const metadata = { title: "Watchlist — Sherlog" };

export default function WatchlistPage() {
  return (
    <RoutePlaceholder
      title="Watchlist"
      description="Tokens tracked for follow-up."
    />
  );
}
