import RoutePlaceholder from "@/components/ui/RoutePlaceholder";

export const metadata = { title: "Signals — Microcap Engine" };

export default function SignalsPage() {
  return (
    <RoutePlaceholder
      title="Signals"
      description="Active setup and signal history."
    />
  );
}
