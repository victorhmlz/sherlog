import RoutePlaceholder from "@/components/ui/RoutePlaceholder";

export const metadata = { title: "Scanner — Microcap Engine" };

export default function ScannerPage() {
  return (
    <RoutePlaceholder
      title="Scanner"
      description="Live token discovery and filtering."
    />
  );
}
